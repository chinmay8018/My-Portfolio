import { useEffect, useState } from 'react'

/**
 * useReducedMotion
 * Returns true if the user prefers reduced motion.
 * Use this to skip heavy 3D / GSAP animations and render a static fallback.
 */
export function useReducedMotion() {
  return false
}
